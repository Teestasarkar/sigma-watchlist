{
  "name": "sigma-watchlist",
  "private": true,
  "version": "1.0.0",
  "description": "A market watchlist that answers 'what meaningfully changed since I last looked?'",
  "workspaces": ["server", "web"],
  "engines": { "node": ">=20.11" },
  "scripts": {
    "dev": "concurrently -k -n api,web -c cyan,magenta \"npm:dev:api\" \"npm:dev:web\"",
    "dev:api": "npm run dev --workspace server",
    "dev:web": "npm run dev --workspace web",
    "build": "npm run build --workspace server && npm run build --workspace web",
    "start": "npm run start --workspace server",
    "test": "npm run test --workspace server",
    "typecheck": "npm run typecheck --workspace server && npm run typecheck --workspace web",
    "seed": "npm run seed --workspace server",
    "reset": "npm run reset --workspace server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
